# Vercel: set in Project → Settings → Environment Variables

# Production API (Railway custom domain)
VITE_API_URL=https://api.yourdomain.com/api

# Local dev: leave unset to use Vite proxy (/api → localhost:3000)
